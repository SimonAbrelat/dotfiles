# Dotfiles #

This is the dotfiles for:
- Bash
- Tmux

The goal is to have a minimal and self-contained setup for basic bash and tmux: no ZSH, no jumping, no external plugins.
Rosepine is the color scheme with the ability to easily switch between dark and light themes.

